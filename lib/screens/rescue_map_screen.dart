import 'package:flutter/material.dart';

class RescueMapScreen extends StatelessWidget {
  const RescueMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Rescue Map Screen'),
      ),
    );
  }
}
